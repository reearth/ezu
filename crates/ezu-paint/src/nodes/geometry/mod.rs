//! Geometry ops — `Features -> Features` transforms (centroid, buffer,
//! simplify, …) inspired by turf.js.

mod boundary;
mod buffer;
mod centroid;
mod convex_hull;
mod dash;
mod hatch;
mod simplify;
mod wave;
